# Copy to an ignored file under tmp/do-demo/ and replace every value only after
# the controlled cloud ledger approves the exact revision and resource names.
# This must be the exact generated *.ondigitalocean.app App URL, not a custom
# domain or arbitrary HTTPS endpoint.
APP_AGENT_CONTEXT_URL=https://your-app.ondigitalocean.app/internal/agent/context
FUNCTION_TO_APP_TOKEN=replace-with-a-generated-function-to-app-token
FUNCTION_WEB_SECRET=replace-with-a-generated-secure-web-function-secret
